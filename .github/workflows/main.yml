name: Daily Safety Check
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  check-system:
    runs-on: ubuntu-latest
    steps:
      - name: Run Greeting
        run: |
          echo "Hello GitHub!"
          echo "This is a safe automated task to verify my account activity."
          echo "Current Date: $(date)"
